# yaml-language-server: $schema=https://json.schemastore.org/yamllint.json
app: par2cmdline-turbo
base: false
description: par2cmdline-turbo is a fast, standalone PAR 2.0 compatible file verification and repair tool.
channels:
- name: stable
  platforms: ["linux/amd64", "linux/arm64"]
  stable: true
  tests:
    enabled: true
    type: cli
