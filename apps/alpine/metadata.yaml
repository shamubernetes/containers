# yaml-language-server: $schema=https://json.schemastore.org/yamllint
app: alpine
base: true
description: A minimal Docker image based on Alpine Linux used as a base image for other containers.
channels:
- name: "3.19"
  primary: true
  platforms: ["linux/amd64", "linux/arm64"]
  stable: true
  tests:
    enabled: true
    type: cli
