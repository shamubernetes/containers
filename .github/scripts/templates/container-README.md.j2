<!---
NOTE: AUTO-GENERATED FILE
to edit this file, instead edit its template at: ./github/scripts/templates/container/README.md.j2
-->
<div align="center">

# {{ image.name }}

</div>

{{ image.description }}


{{
{% if image.environment %}

## Custom environment configuration

This container support setting certain custom environment variables with the use of [drone/envsubst](https://github.com/drone/envsubst).

{% set name_length = 5 %}
{% set default_length = 8 %}
{for env in image.environment %}
{% set line_name_length = env.name|length %}
{% if line_name_length > name_length %}
{% set name_length = ( line_name_length + 1) %}
{% set line_default_length = env.default|length %}
{% if line_default_length > default_length %}
{% set default_length = ( line_default_length + 1) %}
{% endif %}
{% endfor %}


| Name{{ " " * (name_length - 4)}}| Default |
|-{{ "-" * name_length }}|-{{ "-" * default_length }}|
{% for env in image.environment %}
| {{ env.name }}{{ " " * (name_length - env.name|length) }}| {{ env.default }}{{ " " * (default_length - env.default|length) }}|
{% endfor %}
{% endif %}
}}

{{
{% if image.dockerfile %}
### Dockerfile
```Dockerfile
{{ image.dockerfile }}
```
{% endif %}
}}
