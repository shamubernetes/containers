# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Pull Request: Validate"

on:
  pull_request:
    branches: ["main"]
    types: ["opened", "synchronize", "reopened"]

concurrency:
  group: ${{ github.head_ref }}-pr-validate
  cancel-in-progress: true

jobs:
  simple-checks:
    uses: shamubernetes/containers/.github/workflows/simple-checks.yaml@c4112716ce7d8da2bb7f80bdaabe92698dec35b2 # main

  get-changed-images:
    uses: shamubernetes/containers/.github/workflows/get-changed-images.yaml@c4112716ce7d8da2bb7f80bdaabe92698dec35b2 # main

  build-images:
    needs: ["simple-checks", "get-changed-images"]
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: shamubernetes/containers/.github/workflows/build-images.yaml@c4112716ce7d8da2bb7f80bdaabe92698dec35b2 # main
    secrets: inherit
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: false
      sendNotifications: false
